# .gitignore for project_plp-2025
# Ignore virtual environments
venv/
.venv/
ENV/
env/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/version info
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage
.pytest_cache/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*,cover

# Translations
*.mo
*.pot

# IDEs and editors
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs and runtime data
logs/
*.log
*.out
*.err

# Data, models and outputs (customize as needed)
# Raw data and large binary caches
data/raw/
# Local caches or temporary data
data/tmp/
# Model checkpoints and heavy binaries
models/checkpoints/
models/*.keras
models/*.h5
models/*.ckpt

# Visualizations and generated outputs
visualization/outputs/
outputs/

# Secrets and config files
.env
.env.*
secrets.yml
secrets.json
*.pem
*.key
*.crt
config/*.local.py
config/*.secret.py

# Notebook checkpoints and outputs (ignore if you keep notebooks in repo)
.ipynb_checkpoints/
*.ipynb

# Temporary files
*~
*.tmp
*.bak
*.swp
*.swm
*.orig

# Local development / temporary Python scripts
# Patterns for debug/dev helper scripts which are not part of the production project
# Add or remove as you see fit. Examples included below.
*_debug.py
*dev_*.py
local_*.py
*_local.py

# Common informal developer test files (adjust if your test files are part of repo)
# Example files we often don't want to push
run_app_debug.py
test_simple_server.py

# Markdown: drafts, local notes, backups
# If you keep documentation in repo, DO NOT add blanket rules here — instead add specific drafts.
*.draft.md
*.local.md
*_notes.md
notes/
*.md~

# Ignore all Markdown files by default, except the canonical project overview
# This keeps working docs and drafts out of the repository while preserving
# the main `PROJECT_OVERVIEW.md` file that should remain tracked.
*.md
!PROJECT_OVERVIEW.md
# Optional: ignore sensitive exported config or reports
reports/

# If you accidentally committed something and want to remove it from tracking, use:
#   git rm --cached <file>
#   git commit -m "Remove sensitive/large file from repo"
# To remove from history entirely (use with caution): see 'git filter-repo' or 'BFG Repo-Cleaner'

# === End of file ===
